var Base = prompt ("Saquemos el área de un triangulo, dime el tamaño de la Base")  
var Altura = prompt ("Dame el tamaño de la altura")
var R = (parseInt(Base) * parseInt(Altura)) / 2;

alert("El resultado del área del triangulo es " + R)
